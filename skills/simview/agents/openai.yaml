interface:
  display_name: SimView
  short_description: Review a live mobile device
  default_prompt: Use $simview to open an available iOS Simulator or Android device and review the current screen.
dependencies:
  tools:
    - type: mcp
      value: simview
      description: SimView local mobile-device preview and controls
