{
  "name": "@simview/cli",
  "version": "0.1.10",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "exports": "./src/index.ts",
  "dependencies": {
    "@simview/client": "workspace:*",
    "@simview/core": "workspace:*",
    "@simview/mcp": "workspace:*"
  },
  "scripts": {
    "build": "bun build src/index.ts --target=bun --outdir=dist",
    "compile": "bun build --compile --minify --target=bun-darwin-arm64 src/index.ts --outfile=dist/simview",
    "test": "bun test"
  }
}
